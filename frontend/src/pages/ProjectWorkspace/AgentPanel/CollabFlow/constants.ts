/**
 * CollabFlow/constants — 协作流程组件常量
 *
 * 职责：定义组件中使用的样式常量和配置值。
 */

/** 并行节点最大数量（同一层） */
export const MAX_PARALLEL_NODES = 3;

/** 并行节点最大数量的提示文案 */
export const MAX_PARALLEL_NODES_MESSAGE = '同一层最多并行 3 个节点';
